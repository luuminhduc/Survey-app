export default class Survey{
    constructor(title, numberOfQuiz, quizList, users) {
        this.title = title;
        this.numberOfQuiz = numberOfQuiz;
        this.quizList = quizList;
        this.users = users;
    }
}